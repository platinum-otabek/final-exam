apiVersion: v1
kind: Pod
metadata:
  labels:
    author: otabek
    last: final
  name: nginx1
  namespace: otabek
spec:
  containers:
  - image: nginx
    name: nginx1
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
